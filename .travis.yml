language : node_js
node_js:
 - "10"
install:
- npm install
os:
 - linux

script:
 - echo 'start auto build'

jobs:
  include:
    - stage: npm release
      script: npm run build
      deploy:
        provider: npm
        email: "$EMAIL"
        api_key: "$TOKEN"
        skip_cleanup: true